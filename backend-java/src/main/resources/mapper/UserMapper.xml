<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.agenthub.mapper.UserMapper">
  <resultMap id="UserResultMap" type="com.agenthub.model.User">
    <id property="id" column="id" />
    <result property="username" column="username" />
    <result property="passwordHash" column="password_hash" />
    <result property="displayName" column="display_name" />
    <result property="avatarUrl" column="avatar_url" />
    <result property="createdAt" column="created_at" />
    <result property="updatedAt" column="updated_at" />
  </resultMap>

  <insert id="insert" parameterType="com.agenthub.model.User">
    INSERT INTO users (id, username, password_hash, display_name, avatar_url)
    VALUES (#{id}, #{username}, #{passwordHash}, #{displayName}, #{avatarUrl})
  </insert>

  <select id="findByUsername" resultMap="UserResultMap">
    SELECT id, username, password_hash, display_name, avatar_url, created_at, updated_at
    FROM users
    WHERE username = #{username}
  </select>

  <select id="findById" resultMap="UserResultMap">
    SELECT id, username, password_hash, display_name, avatar_url, created_at, updated_at
    FROM users
    WHERE id = #{id}
  </select>

  <update id="update" parameterType="com.agenthub.model.User">
    UPDATE users
    SET display_name = #{displayName},
        avatar_url = #{avatarUrl}
    WHERE id = #{id}
  </update>
</mapper>
